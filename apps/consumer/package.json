{
  "name": "@optio/consumer",
  "version": "1.0.0",
  "description": "Independent RabbitMQ Event Stream Consumer with effectively-once deduplication and DLQ routing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/**/*.spec.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@optio/shared": "*",
    "amqplib": "^0.10.4",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
